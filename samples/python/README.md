# WeaveHeap Python Sample (scaffold)

Build the Python extension using maturin, then run the sample:

```
# From crates/weaveheap-python
maturin develop

# Then
python3 ../../samples/python/main.py
```
